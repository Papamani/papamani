#include<stdio.h>
#include<string.h>
int main()
{
int f,i;
char str[100],l;
printf("Enter the paragraph");
scanf("%s",&p);
for(i = 0; i <= l-1; i++)
   {
      if(str[i] == ' ')
      {
    f = f + 1;
      }
   }
   printf("The number of lines in the string are %d", f);
return 0;
}

	   
